{
  "name": "twitter-tracker",
  "private": true,
  "scripts": {
    "test": "node --test",
    "check": "node --check background.js && node --check content.js && node --check popup.js && node --check sites.js"
  }
}
